// Step 1: Create the array
let numbers = [1, 2, 3, 4, 5];

// Step 2: Remove the last element using pop()
numbers.pop();

// Step 3: Print the modified array to the console
console.log(numbers);
