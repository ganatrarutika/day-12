// Step 1: Create the array with initial colors
let colors = ["red", "green", "blue"];

// Step 2: Add "yellow" to the beginning of the array
colors.unshift("yellow");

// Step 3: Print the resulting array to the console
console.log(colors);
