// Step 1: Create an array called colors with the names of three different colors
const colors = ['Red', 'Green', 'Blue'];

// Step 2: Use a loop to print each color to the console
for (let i = 0; i < colors.length; i++) {
    console.log(colors[i]);
}

