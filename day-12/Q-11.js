// Step 1: Define the array
let numbers = [1, 2, 3, 4];

// Step 2: Use the map function to double each element
let doubledNumbers = numbers.map(number => number * 2);

// Step 3: Print the resulting array to the console
console.log(doubledNumbers);

